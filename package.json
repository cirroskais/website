{
	"name": "cirroskais",
	"version": "0.0.1",
	"private": true,
	"scripts": {
		"start": "node build/",
		"dev": "vite dev",
		"build": "vite build",
		"preview": "vite preview",
		"lint": "prettier --plugin-search-dir . --check .",
		"format": "prettier --plugin-search-dir . --write .",
		"docker:build": "docker build . -t cirroskais/www",
		"docker:run": "docker-compose up",
		"docker:run -d": "docker-compose up -d"
	},
	"devDependencies": {
		"@sveltejs/adapter-auto": "next",
		"@sveltejs/kit": "next",
		"autoprefixer": "^10.4.13",
		"postcss": "^8.4.19",
		"prettier": "^2.6.2",
		"prettier-plugin-svelte": "^2.7.0",
		"svelte": "^3.44.0",
		"svelte-preprocess": "^4.10.7",
		"tailwindcss": "^3.2.4",
		"vite": "^3.1.0"
	},
	"type": "module",
	"dependencies": {
		"@sveltejs/adapter-node": "^1.0.0-next.101"
	}
}
